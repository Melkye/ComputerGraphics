namespace ImageConverter.Gif;

public class LzwCompresser
{
    public byte[] Compress(byte[] data, int codeSize) => throw new NotImplementedException();
    public byte[] Decompress(byte[] data, int codeSize)
    {
        Dictionary<byte, byte> colorsDic = new();

        return Array.Empty<byte>();
    }
}
